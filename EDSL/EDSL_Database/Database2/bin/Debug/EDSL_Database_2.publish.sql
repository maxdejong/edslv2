/*
Deployment script for EDSL

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "EDSL"
:setvar DefaultFilePrefix "EDSL"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
PRINT N'Creating [dbo].[Ladder]...';


GO
CREATE TABLE [dbo].[Ladder] (
    [LadderId]   INT NOT NULL,
    [divisonPID] INT NULL,
    PRIMARY KEY CLUSTERED ([LadderId] ASC)
);


GO
PRINT N'Creating [dbo].[LadderPos]...';


GO
CREATE TABLE [dbo].[LadderPos] (
    [LadderPosId]  INT NOT NULL,
    [LadderID]     INT NOT NULL,
    [TeamID]       INT NOT NULL,
    [Played]       INT NOT NULL,
    [Wins]         INT NOT NULL,
    [losses]       INT NOT NULL,
    [draws]        INT NOT NULL,
    [goalsFor]     INT NOT NULL,
    [goalsAgainst] INT NOT NULL,
    PRIMARY KEY CLUSTERED ([LadderPosId] ASC)
);


GO
PRINT N'Creating unnamed constraint on [dbo].[Ladder]...';


GO
ALTER TABLE [dbo].[Ladder] WITH NOCHECK
    ADD FOREIGN KEY ([divisonPID]) REFERENCES [dbo].[Division] ([divisionID]);


GO
PRINT N'Creating unnamed constraint on [dbo].[LadderPos]...';


GO
ALTER TABLE [dbo].[LadderPos] WITH NOCHECK
    ADD FOREIGN KEY ([LadderID]) REFERENCES [dbo].[Ladder] ([LadderId]);


GO
PRINT N'Creating unnamed constraint on [dbo].[LadderPos]...';


GO
ALTER TABLE [dbo].[LadderPos] WITH NOCHECK
    ADD FOREIGN KEY ([TeamID]) REFERENCES [dbo].[Team] ([teamID]);


GO
